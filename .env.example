DB_ROOT_PASSWORD=rootpassword
DB_NAME=torn_market
DB_USER=torn
DB_PASSWORD=tornpassword
DB_PORT=3306
ADMIN_PASSWORD=secretadmin
TORN_API_KEY=your_torn_api_key_here

# === Database Profile ===
# Use 'internal' to run the bundled MySQL container (default for local dev).
# Use 'external' to connect via Tailscale proxy to a remote DB.
COMPOSE_PROFILES=internal

# === External DB (Tailscale) Settings ===
# Required only when COMPOSE_PROFILES=external
# REMOTE_DB_HOST=100.90.10.0   # Tailscale IP of your remote MySQL server
# TS_AUTHKEY=tskey-auth-xxxxx  # Tailscale Auth Key
